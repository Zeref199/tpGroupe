spring:
  application:
    name: TpGroupe-obs-api

elasticsearch:
  host: beyond-log-prod.es.cegedim.cloud
  port: 443
  scheme: https
  user: admin
  pass: jaeger-tpgroup-prod

management:
  health:
    elasticsearch:
      enabled: false