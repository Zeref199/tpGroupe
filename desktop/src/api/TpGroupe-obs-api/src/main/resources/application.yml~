spring:
  application:
    name: TpGroupe-obs-api

server:
  port: 8081

# ── Production OpenSearch (read-only) ────────────────────────────────────────────
elasticsearch:
  host: beyond-log-prod.es.cegedim.cloud
  port: 443
  scheme: https
  user: admin
  pass: mVC%w=EzLo3+

storage:
  elasticsearch:
    host: beyond-log-dev-qa.es.cegedim.cloud
    port: 443
    scheme: https
    user: admin
    password: "ee4ek<yM!h*)"
    usePassword: true
    indexPrefix: dev-obs
    extraEnv:
      - name: ES_USE_ILM
        value: "true"
      - name: ES_USE_ALIASES
        value: "true"
      - name: ES_CREATE_INDEX_TEMPLATE
        value: "false"

# ── Ingestion polling configuration ────────────────────────────────────────────
poll:
  delay:
    ms: 30000              # how often (in ms) to poll for new spans

management:
  health:
    elasticsearch:
      enabled: false